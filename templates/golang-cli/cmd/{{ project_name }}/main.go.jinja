package main

import (
	"fmt"
	"os"

	"{{ module_path }}/internal/cli"
)

func main() {
	if err := cli.NewRoot().Execute(); err != nil {
		fmt.Fprintln(os.Stderr, "{{ project_name }}:", err)
		os.Exit(1)
	}
}
